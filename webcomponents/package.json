{
  "license": "MIT",
  "scripts": {
    "add": "bower install --save",
    "prebuild": "bower update",
    "build": "polymer build",
    "deploy": "firebase deploy --only hosting"
  },
  "devDependencies": {
    "bower": "^1.8.4",
    "firebase-tools": "^3.19.3",
    "polymer-cli": "^1.7.7"
  }
}
